TLALPOWA · verificacion arranque Z10
Fecha: 2026-06-14

Cambios verificados por lectura de fuente:
- kMapStartupTileZoom = kMapOverviewTileZoom = Z10.
- kStartupSplashSolidSeconds = 5.40 s; kStartupSplashMaximumSeconds = 22.00 s.
- Satelite embebido Z10 cubre Z1-Z10 mediante satellite_embedded_z10_covers_view().
- draw_tile_layer() retorna temprano en satelite Z1-Z10 y evita ordenar/solicitar teselas bajas.
- visible_tile_coverage_ratio() acepta respaldo embebido como cobertura completa visible.
- map_loader inicia despues del primer frame de bienvenida y ya no espera hotdata/catalogos.
- Se conserva Z15 como respaldo de detalle y el streaming limitado por presupuesto de pantalla.

Validacion ejecutada en contenedor:
- gcc -std=c11 -fsyntax-only tlalpowa_c.c: OK.
- g++ -std=c++17 -fsyntax-only -I. Tlalpowa.cpp: OK.
- tlalpowa_nucleo.cpp no se validó aqui por falta de nlohmann/json.hpp del toolchain local.
